import { TProduct } from "../../types/product.types";
import product from "../../assets/images/E-product.webp";

const ProductCard = (data: TProduct) => {
  return (
    <div>
      <h2>Welcome to the ProductCard page</h2>
    </div>
  );
};

export default ProductCard;
